version: 1

services:
  - type: web
    name: cloud-erp-gateway
    runtime: docker
    dockerfilePath: gateway/Dockerfile
    rootDirectory: cloud-erp-platform
    plan: free
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /
